use super::{Coordinate, Color, Cell, HashMap};

fn rook_move(from: Coordinate, status: HashMap<String, Cell>) -> Vec<Coordinate> {
    let x = from.x();
    let y = from.y();

    let mut to = vec![];

    let mut dx = x;
    let mut dy = y;

    loop {
    }

    to
}